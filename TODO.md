# TODO

- [ ] slim down bundle and logic as much as possible
- [ ] move network requests from .ts to rust through `invoke`˙s (see https://tauri.app/develop/calling-rust/)
- [x] build for Windows to test locally
- [ ] build for Ubuntu to test on a Raspberry
